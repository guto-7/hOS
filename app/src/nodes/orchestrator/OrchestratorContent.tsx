function OrchestratorContent() {
  return (
    <p style={{ color: "var(--text-muted)" }}>
      Cross-diagnostic analysis will appear here once multiple nodes have
      results. The orchestrator aggregates output contracts from all nodes.
    </p>
  );
}

export default OrchestratorContent;
