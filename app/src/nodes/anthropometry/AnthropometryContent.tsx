function AnthropometryContent() {
  return (
    <p style={{ color: "var(--text-muted)" }}>
      Body composition node not yet implemented. Manual measurements and
      analysis will be available once the anthropometry node is built.
    </p>
  );
}

export default AnthropometryContent;
