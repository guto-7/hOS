# hOS Python dependencies
# Install: cd data && python3 -m venv .venv && .venv/bin/pip install -r requirements.txt

# Image processing
numpy>=1.21.0
Pillow>=9.0.0
opencv-python>=4.5.0
scikit-image>=0.19.0

# Deep learning (imaging pipeline)
torch>=1.13.0
torchvision>=0.14.0
torchxrayvision>=0.0.28
ultralytics>=8.0.0

# Claude API (interpretation + body part detection)
anthropic>=0.7.0
python-dotenv>=0.19.0
